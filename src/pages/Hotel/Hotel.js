import React from 'react';
import Header from '../Navbar/Header';

const Hotel = () => {
    return (
        <div>
            <Header type="list"></Header>
        </div>
    );
};

export default Hotel;